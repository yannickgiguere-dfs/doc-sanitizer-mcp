# Doc Sanitizer MCP Server Configuration
# Copy this file to .env and customize as needed

# Ollama Configuration
# Change the model without rebuilding the Docker image
# Options: phi4:14b (default), qwen2.5:7b, llama3.2:3b
OLLAMA_MODEL=phi4:14b
OLLAMA_HOST=http://ollama:11434

# MCP Server Configuration
PORT=8000
LOG_LEVEL=INFO

# Profile Storage (inside container, mapped to host via volume)
PROFILE_STORAGE=/app/data/profiles.json
